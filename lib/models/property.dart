class Property {
  final String type; // house, apartment, warehouse, farmhouse
  final String areaSize;
  final String measurementUnit; // square meters, square feet, etc.
  final double price;

  Property(this.type, this.areaSize, this.measurementUnit, this.price);
}
